'use client';

import React from 'react';

export default function ConsultationClient() {
    return (
        <div>
            <button onClick={() => alert('Booking consultation...')}>
                Book Now
            </button>
        </div>
    );
}
